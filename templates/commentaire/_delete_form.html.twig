<form method="post" action="{{ path('app_commentaire_delete', {'idCom': commentaire.idCom}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.idCom) }}">
    <button class="btn">Delete</button>
</form>
