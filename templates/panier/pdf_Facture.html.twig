<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Product List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 20px;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        table {
            width: 100%;
        
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
            font-weight: bold;
            color: red; /* Set th text color to red */
        }

        tbody tr:hover {
            background-color: #f9f9f9;
        }

        .category-column {
            width: 20%;
        }

        .price-column {
            width: 15%;
        }

        .description-column {
            width: 35%;
        }

        .name-column {
            width: 20%;
        }

        .currency {
            font-weight: bold;
        }

        .invoice-container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .invoice-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .logo img {
            max-height: 60px;
        }

        .client-info {
            text-align: left;
        }

        .date-time {
            text-align: right;
        }

        .invoice-total {
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="invoice-container">
        <div class="invoice-header">
            <div class="logo">
                <img src="{{ asset('images/logo.jpg') }}" alt="Logo">
            </div>
            <div class="date-time">Agence Name: Affariety</div>
            <div class="date-time">Client Name: {{ user.nom }} {{ user.prenom }}</div>
   
            <div class="date-time">Date: {{ dateSysteme|date('d/m/Y H:i:s') }}</div>
        </div>
        <table >
            <thead>
                <tr>
                    <th>Product Name</th>
                    <th>Price</th>
                </tr>
            </thead>
            <tbody>
                {% for product in products %}
                    <tr>
                        <td>{{ product.nomP }}</td>
                        <td>{{ product.prixP }}</td>
                    </tr>
                {% endfor %}
                <tr>
                    <td colspan="2" class="invoice-total">Total: {{ totalPrix }}</td>
                </tr>
            </tbody>
        </table>
    </div>
</body>
</html>
